# Generated by tools/build-site.js — GitHub Actions rebuilds it on every push,
# so there is no reason to carry rendered PNGs in git history.
site/

# Rendered output from the editor and MCP export_sprite. Regenerable from library/.
exports/

node_modules/
*.log
.DS_Store
